import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Typography textAlign="center" m={10} variant="h6">
        Homepage
      </Typography>
    </Box>
  );
};

export default Home;
